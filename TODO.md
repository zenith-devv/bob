1. Reduce binary sizes (currently ~75 MB - 80 MB)
2. Add bob.db and bob install (puts the project in $PATH and db for managing installed programs, the ability to `bob install x` `bob remove x` `bob reinstall x`)
